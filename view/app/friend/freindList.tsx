import { useEffect } from "react"

export default async function freindList(id: {id: string}) {
    /** 친구 목록 통신 */
    useEffect(() => {
        
    }, [])
    return(
        <div className="freindList">
            {/** 이름, 접속 상태(마지막 접속 시각), 친구 삭제, 채팅창 열기 및 닫기*/}
            <p>frindList</p>
        </div>
    )
}